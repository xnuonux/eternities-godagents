{
  "schemaVersion": 1,
  "realmId": "fixture-workbench",
  "version": "1",
  "trustModel": "fixture-local",
  "capabilities": ["counter.observe", "counter.increment"],
  "observations": [
    { "id": "counter.state", "freshness": "cycle" }
  ],
  "hands": [
    {
      "id": "counter.increment",
      "effect": "local-write",
      "requiredAuthority": ["realm:write"],
      "idempotent": true,
      "inputSchema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["amount"],
        "properties": {
          "amount": { "type": "integer", "minimum": 1, "maximum": 1 }
        }
      },
      "expectedOutcome": {
        "type": "observation-delta",
        "fields": {
          "counter": { "observationField": "counter", "addInputField": "amount" }
        }
      }
    }
  ],
  "resources": {
    "maxActionsPerCycle": 1
  },
  "privacy": {
    "retention": "test-only"
  },
  "lifecycle": {
    "entry": "explicit",
    "suspension": "fail-closed",
    "recovery": "reconcile",
    "exit": "receipt-required"
  },
  "compatibleDistributions": ["0.1.x"]
}
