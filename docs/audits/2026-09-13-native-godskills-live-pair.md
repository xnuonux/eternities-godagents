# Native Godskills live history-query pair

Status: both frozen trials ended in provider-stream failures before editing.
The paired quality/efficiency question is **inconclusive**. Two reporting
corrections discovered by the experiment are implemented separately below.

## Question and preregistration

Does the default selected Godskills package improve one real multi-file coding
outcome when the persistent Godagent body, native host, model, tools, initial
source and task are held fixed? This is not Godagent versus plain Pi and not a
general model ranking.

The [plan](../superpowers/plans/2026-09-13-native-godskills-live-pair.md) was
committed as `32746ce` before inference. Baseline source is
`7959e3cef2bf81fa5cebcdc76482fe2aa90d8b44`. Private evidence is under
`D:/00-INDEX/operations/2026-09-13-native-godskills-live-pair`.

Both workspaces contain506 byte-identical tracked files totaling3,490,002bytes.
The full runtime and fixture/schema closure is available for native retrieval,
not serialized into a prompt. Twenty existing reader/config/history-CLI tests
pass in each copy. Actual Pi0.85.1/xAI Grok4.6 OAuth preflight succeeds for both.

The real task adds `history --only/--after/--limit`, its reader and operator
interfaces, selected-row token totals, strict validation before selection,
legitimate Godskills failure-category compatibility, focused tests and docs.
The same exact task text goes to both agents. No existing tests may be changed.
Only the named history/config/operator/CLI/docs paths and new focused tests may
change. No network, dependency installation, personal keel or outside source is
part of the trial.

## Fixed conditions

- Two fresh admissions of the same creation blueprint and verified prompt
  edition, same mission and host effects, no global configuration change.
- Existing native Pi operator and tool loop, Grok4.6 subscription only.
- Per arm:15minute deadline,120native tools,16,384max output setting per
  response. Mission total-token fields are not certified spending ceilings.
- One launch per arm, no retry or continuation prompt, all failures preserved.
- Random order selected before calls: **skills, control**. Sequential dispatch
  avoids shared inference concurrency, but does not eliminate order/cache bias.
- Control omits the optional Godskills binding. Treatment uses existing pinned
  release roots and automatic evidence-qualified activation, no forced method.
  The no-inference probe chose Forge and Mnemosyne in guardrail mode, no method
  body and no deferred review. Actual session selection remains to be checked.
- Arm IDs and filesystem paths differ. The study is not blinded. One pair has
  no statistical replication and cannot demonstrate universal improvement.

## Independent acceptance

A separate Codex worker authored checks from the task contract without writing
the implementation. Owner review corrected an erroneous leading-zero parser
expectation and a tie-fixture/count mismatch **before** freezing or inference.
The final20 tests check real temporary files and reader/operator/CLI callers,
not mocked query output. Baseline is **9pass/11fail** in both copies. Old behavior
passing is retained; only the complete post-change suite can meet acceptance.

Freeze time:2026-09-13T20:21:32.694Z. Skills dispatch began20:21:33.019Z.
Canonical frozen record digest:
`8e86edd25b1cbd48f4f194e63fc02c44e7097cab9d2c8f27ec57b68f975362a8`.
Task SHA256:
`018f14c7e3516cd6af7ce32a11ba804df384efbd797aca6944d7234a99fa859c`.
Acceptance test SHA256:
`46fc59708dde2f66989404d8978718be8e71397ca167949867ee969009506bf1`.
Helper SHA256:
`91204872977f5af14f92bf863155f37527ece802b67d5091b268d23a64943c24`.
Source manifest digest:
`d25ad903ff364c3213066f6b0e86f8a9a3e3e689ff077e0dc4e60cd7f6a78e24`.

Primary endpoint requires a normally settled turn,20/20 acceptance,20/20
unchanged regressions and no out-of-scope changes. Record test counts, elapsed
time, native calls/tools and provider-reported input/output/cache/total without
double-counting cache or inferring money/hidden reasoning. Unknown counters
remain unknown. Failed output is scored unchanged and never repaired first.

Neither native tools nor outside-workspace test placement create OS isolation.
The latter is experimental separation from ordinary task retrieval, not a
malicious-agent security guarantee. The comparison does not certify production
Realm, automated review phases, Soul/Inspiration, Lunari or all-model support.

## Skills arm observation

Actual selected capabilities are Forge and Mnemosyne, guardrail mode. The native
session is `01a09c6e-ad71-7772-b730-9cb02720dc15`. It completed17read/search/list
tools without errors over five successful provider messages; the sixth inference
ended with `native-pi:provider-failed`. The native transcript's error message is
only `terminated`. No files changed. State returned idle, with no revocation,
pending actions or compaction, after100.247seconds including setup. The15minute
deadline had not expired. This is an observed provider-stream failure, not an
established network cause or a Godskills quality failure.

Unmodified output scores9/20 acceptance and20/20 existing regression, with no
in-workspace scope violation. Primary endpoint fails. Reported counters sum to
28,243input,912output,32,512cache-read,0cache-write,61,667total. However, the failed
message has all-zero counters. The installed Pi Responses adapter initializes
those counters to zero before streaming and can emit them unchanged on an error
(`pi-ai/dist/api/openai-responses.js`, stream initialization/catch). Therefore
61,667 is a reported subtotal, **not proof of complete consumption**. The current
collector's `missingUsageMessages:0` does not detect that sentinel. Do not infer
zero cost for the failed call or treat this as an efficiency result.

The actual native association binds record
`ad6f5e4143d0f84ad876332c4ad5aac7e8ac9a3265caa3a91546adf9e855c84f`,
whose selected packages are exactly Forge/Mnemosyne guardrails and zero deferred
reviews. Control has neither the Godskills config option nor a skill binding in
its native association. This confirms treatment delivery, not coding benefit.

## Paired outcome

| Observation | Skills | Control |
| --- | ---: | ---: |
| Independent acceptance | 9/20 | 9/20 |
| Unchanged focused regressions | 20/20 | 20/20 |
| Native inferences | 6 | 8 |
| Completed tools | 17 | 21 |
| Tool errors | 0 | 1 |
| Changed files | 0 | 0 |
| Elapsed including setup | 100.247s | 238.756s |
| Reported input subtotal | 28,243 | 34,201 |
| Reported output subtotal | 912 | 7,037 |
| Reported cache-read subtotal | 32,512 | 72,064 |
| Reported cache-write subtotal | 0 | 0 |
| Reported total subtotal | 61,667 | 113,302 |
| Complete consumption known | no | no |

Control session `01a09c70-716a-7299-a894-cef2f3647760` stopped with
`native-pi:provider-failed`; its native error is
`Error Code null: Internal error during token generation`. Its final failed
message also contains the SDK's zero counters. Both actors returned idle with
no pending action, no compaction and no in-workspace scope violation. A grep
tool failed once in control and was followed by successful tools. Neither
deadline expired. No candidate implementation was produced or promoted.

These are unsuccessful completion outcomes, not two completed implementations
with equal quality. No claim that skills caused or prevented the provider
failures follows. The short independent reporting-fix review later succeeded
through the same subscription, so neither universal service outage nor exhausted
authentication/quota is established by these two failures. No fallback or retry
replaced either trial slot. The native host itself was left unchanged throughout
both calls and their frozen scoring.

## Evidence-backed reporting corrections

After both trials finished, owner implementation `a8bc116` fixes two confirmed
issues without rewriting their records:

1. `src/host/native-session-report.mjs` treats error/aborted events with only
   zero/absent counters as unknown cumulative usage. Successful zero usage and
   nonzero reported failure usage remain intact. Three focused cases retain
   event deduplication and exercise the actual collector contract.
2. `src/host/native-run-history.mjs` accepts the existing operator's legitimate
   screened `native-godskills:*` categories. Tests still reject raw diagnostics,
   uppercase/newline/empty suffixes and foreign prefixes.

The new tests first showed2expected failures; the patched integrated native gate
passed **83/83**, no failures/skips,8.390seconds. A read-only replay of both real
transcripts through the corrected collector yields null totals and exactly one
missing-usage message per arm. `corrected-usage-replay.json` records that derived
observation separately; original scores and run records remain byte-for-byte
untouched. Nonzero failure usage is still only reported consumption, not a
new billing guarantee. Historical placeholder-zero records stay historical.

A separate bounded, tool-free Grok4.6 subscription source review returned
**Ready to merge: yes**, with no critical/important finding. All four reviewed
source/test hashes match. Review verdict SHA256 is
`af3b34521f4d3c17da94126a56bd3aa9e7de44af173f8b4dfd2df0630bf27c25`.
Review scope was the two reporting fixes, not a
Godskills quality certification. Its reported usage is10,363input,3,747output,
512cache-read and14,622total; that successful review is not a replacement trial.
Full regression at implementation `a8bc116` passed **1,596tests, zero failures,
nine existing optional browser skips**,775.323seconds. This includes verification
of all69 unchanged historical receipts and their source lineage. Only
documentation changed after the reviewed and full-tested implementation.
Full log SHA256:
`b121ce8af1fa18b615c0345a97a6104f119f14595a0e16b83814dd5e7699bd14`.

## Integration

The verified reporting correction and study evidence were merged and pushed to
main at `41f037a8633236773c81d5cd3307bce79b85770a`. The merged native gate passed
**83/83**, zero failures/skips,9.093seconds. The post-push ref-aware
cross-repository gate passed **8/8**, zero failures/skips,75.646seconds, with
Godskills main/origin at `d0781a91a82898cc808e7fc681f145fdd972dade`. The final
closeout changes documentation only. No historical receipt, frozen trial record,
user package-lock or Godskills release artifact was rewritten. No live trial or
review process remains running.

## Next meaningful step

History query development remains unfinished. Before spending on another quality
comparison, obtain a bounded provider-diagnostic trace for the native generation
failure path, preserving the mature host and source context. A new attempt needs
a separately recorded slot and unchanged or explicitly versioned acceptance,
not deletion/retry of these results. Do not force skill activation, shrink the
task to a toy, raise global token windows, change identity or switch to paid APIs
to manufacture a win. Only then resume the useful history-query task and its
matched outcome study. The existing consumer remains integration-qualified,
not proven to improve quality.
