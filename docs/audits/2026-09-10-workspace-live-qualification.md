# Workspace live qualification: stopped before execution

## Integrated result

Godagents implementation `c116303` and integration record `89978c2` add the actual local workspace application owner. It authenticates a fresh `local-workspace-v3` actor, stages checked model proposals without changing original source, requires separately pinned independent review, runs the reviewed local browser suite, carries actual failed-test feedback into a bounded follow-up, recovers prepared/dispatched/completed operations, and exports checked text only after a pass.

Selected Prompt OS guidance reaches actual native/review/revision sender requests in `a374d27`; the bounded first-party exporter is on Prompt OS master `ad8460d`. This is selected operating guidance, not wholesale adoption of the third-party Astra corpus or a claim about its authenticity.

The final full local suite passed 1493 tests with zero failures and seven explicit runtime-opt-in skips, 520163.7557 ms. Those seven passed separately with actual Chrome: six application/browser cases plus one process-recovery case. Post-merge owner/export/guidance checks passed 27/27. The earlier full run's one older process-test failure remains preserved and unexplained; a subsequent unchanged named case passed, then the full suite passed with improved test-only diagnostics. No runtime timeout or authority boundary was weakened. See [workspace owner implementation and gates](../superpowers/plans/2026-09-10-workspace-owner.md).

## Live experiment and exact outcome

The first new coding comparison was prepared against Godagents `89978c2f09dbd1e0e18443c0b5990acb5e50d398`, Prompt OS `ad8460d7b6c788c84a7a619381d022f4dea17b26`, and unchanged effect-only dependency `3615b355e7dafbf2efe9e6c6bb3373458eba6119`.

- Same first-party task-list source, requirements, proposal schema, independent host review, revision store and browser suite in both arms.
- Native Grok 4.6 subscription, low reasoning, one local dispatch per arm, 8192 inclusive completion tokens per arm, 90-second provider deadline. No retries, OpenRouter, or paid fallback.
- Godagent treatment: fresh fixture-derived actor and two selected first-party guidance modules, continuity and tool-use. Plain arm: the same model/process with a direct task prompt, no actor or Godskills bodies.
- One development sample only. No general quality, production readiness, consciousness, or live iterative-feedback claim.
- Existing native credential readiness was checked without exposing credentials. Binary/bridge bytes matched the previously reviewed pins.
- The initial non-inference setup put the relative-path identity policy at the wrong parent. This was corrected before the registration was frozen; no runtime code, admission, or authority was altered to bypass validation.

Independent reviewer `01a089d3-4ef5-7e81-8624-52bc148db7af` approved the final preparation after tracing a suspected timeout discrepancy. The 30-second identity-policy value bounds the effect-routing adapter, not inference. Actual native dispatch uses the separately pinned 90-second provider timeout in `src/transports/grok-cli-phase-process.mjs`. Both arms had the same inference ceiling. The authoritative registration digest is the locally verified value below, not a transcription in reviewer prose.

The randomized order selected Godagent first. Its one native attempt started at `2026-09-10T05:41:57.631Z`; a durable failure was recorded at `2026-09-10T05:42:12.181Z`. The provider process returned a terminal response, but the screened diagnostic reported `stage: artifact-json` and `textJson: invalid`. The strict transport refused `response-invalid`. There was no accepted artifact, model-generated child revision, browser execution, or source mutation. The plain arm was not dispatched, and no retry was made.

The diagnostic recorded input 16404, output 645, reasoning 185, total 17049. These are **diagnostic counters**, not accepted completion accounting or verified model attribution. Actual charge and physical provider-call count remain unknown. Raw answer text and hidden reasoning were not retained by this diagnostic. Consequently this result does not identify the precise malformed syntax or justify silently extracting code from the answer.

This is an integration failure for this live coding attempt, not a quality win, quality tie, or proof that Grok cannot code. The local application path remains qualified by synthetic-native/real-browser tests. Live Grok coding compatibility and the matched comparison remain incomplete.

## Evidence

Preserved operation directory: `D:/00-INDEX/operations/2026-09-10-grok-workspace-comparison`.

| Record | Digest |
| --- | --- |
| Registration canonical value | `9cbab44e81a0eddc9e02b92506f292db26e00370c4b3e3ea2940af21cb2d3e02` |
| Native dispatch | `6f92091e26e527b382992f202c5c784309d23e1b70c1592e0744719bcb0376e1` |
| Attempt record | `d23bb6a3d9d01e2de9e59d2113204de37c8b0e966b627e46a2d3d99377dcc281` |
| Failure record | `862db33bf9edf0aa4520c22e9ad1dd85506c75b02e1c03c4a6e4f7b07fe069c0` |
| Screened diagnostic record | `e5bc15ec6ddb8c7ad434a9e6c5b802beb61802b792b0024859aee66b00179fb0` |

The registration pins inputs and operational scripts; `result.json` records the stopped sample. Existing attempts and failures must never be overwritten or retried under this identity. The preparation's frozen repository HEADs intentionally describe the tested source, not future documentation commits.

## Next bounded milestone

Qualify structured coding proposals on the existing Grok transport before repeating a quality comparison. Inspect the actual native request contract and current supported structured-output controls. Prefer an explicit, tested provider output contract to permissive parsing. Add focused malformed/valid proposal tests if a real production gap is confirmed, preserve strict authority and exact-preimage validation, and use a new source-pinned bounded sample for any subsequent live attempt. A successful sample must produce an accepted proposal, receive independent review, pass real browser assertions, export checked changes, and replay without another native dispatch. Only then run the matched baseline and compare quality, token accounting and latency.

No new catalog, global prompt bloat, generic scheduler, automatic approval, hostile-code sandbox claim, or Lunari integration belongs in that milestone.
