# Grok structured workspace qualification

## Outcome

The Godagent completed one real local coding task through the production application caller: fresh actor admission, native Grok proposal, exact-revision independent code review, five passing Chrome cases, checked export, and fresh-process replay with new provider invocation blocked. Original source was unchanged. This qualifies that bounded integration path, not broad coding quality, long-horizon reliability or product completion.

The matched plain-model arm did not produce an accepted revision. Its operational failure record is too coarse to establish the precise cause, and accounting was not retained for that downstream failure. Therefore the comparison is **inconclusive**. It is not a Godagent quality win or evidence that the baseline model cannot solve the task. No retry was made.

## Diagnosed omission and narrow change

The prior [stopped attempt](2026-09-10-workspace-live-qualification.md) supplied the output schema only in prompt text. The installed pinned Grok CLI supports `--json-schema`, and the already reviewed Perseus bridge supports translating a schema into that argument. A one-call characterization produced exactly schema-conforming JSON in the ordinary response `text`. The historical malformed answer was not retained, so its exact syntax remains unknown.

Candidate `3ea5f7ef0a6d7ee0bdbdf5e0c541860a01cd6b0b` adds the opt-in `json-schema-v1` profile. It forwards the existing phase schema through the real compiler and pinned process. It does not change the strict parser, phase output contracts, host authority, source preimages, usage accounting, model attribution, retry behavior, or default profile. See [profile and boundary documentation](../grok-structured-output.md).

Independent reviewer `01a08a0f-e7f7-7312-af69-ea15fb4868a5` examined actual producer wiring. An initial concern about the raw host process was resolved by tracing the issued phase port and adding a regression: caller-injected schema, stale descriptor and bridge-substituted schema all reject before inference. The raw host primitive intentionally remains usable for unbound same-model baselines; it is not a model tool or admission authority. Final review found no remaining concrete schema-override route through the actual phase port.

## Live registration

Operation directory: `D:/00-INDEX/operations/2026-09-10-grok-workspace-structured`.

- Godagents source `3ea5f7ef0a6d7ee0bdbdf5e0c541860a01cd6b0b`.
- Prompt OS source `ad8460d7b6c788c84a7a619381d022f4dea17b26`.
- Effect-only dependency `3615b355e7dafbf2efe9e6c6bb3373458eba6119`, unchanged.
- Frozen registration `b26cd0c59242d89512fa50a644519b85ca4deaa51c301d70e39166aa4f30a61b`.
- Native subscription Grok4.6, pinned reported deployment `grok-4.6-build`, low reasoning, one invocation per arm, max8192 inclusive completion tokens per arm, 90-second provider deadline. No retries, OpenRouter, or paid fallback.
- Same broken task-list source, requirements, outer JSON schema control, review opportunity and five browser cases. Randomized order selected Godagent first.
- One previously seen fixture family, not a held-out benchmark. No live multi-step revision or comparative quality claim.

## Actual Godagent proof

The model changed only the task filter from unconditional acceptance to checking the task's completion flag in completed-only mode. The Codex coordinator independently read the complete1106-byte child and recomputed its file SHA before approving that exact browser candidate. Labels, selectors, initial records, nonempty trimmed Enter addition and input clearing were preserved. No external imports, network requests, filesystem operations or dependencies were added.

| Evidence | Value |
| --- | --- |
| Native dispatch | `efb049667e4c2d390da8a8bd4c5c3fcb9713957dad560271d2b0e0c4efbd55e0` |
| Accepted native completion | `63a53322c98ebf933d76257c0b959e1f2210f5c98e42786c91871fe949d00808` |
| Child revision | `0e2b6b13efa2fca44700216089641ceba30c655e72087eb5a0ee9e14b8f93711` |
| Child file SHA256 | `a141b40f400f9f98e9a21c356b33f87ef4cab8117edd8cbcacdca93ebcb5ebeb` |
| Review candidate | `d0122621ad4c7f0f85faa78fc75d9b4799f7d645eeda1d68f8779cd7630feb25` |
| Passed browser receipt | `f4e76979b51834e110012e807f18348af56d0656543678e257450c1da9bf2773` |
| Checked export | `bad2fb951e6687a1db14ee77d231e3ccf9a016a03dfa479ab895ef5eb26c951c` |

The five browser cases were initial rendering, completed-only filtering, Enter addition, blank-input rejection, and restoring all tasks. Native inference plus staging took59089ms; browser plus export5384ms. The verified provider ledger reports16477 input tokens,0 cached input,2957 completion tokens:2512 reasoning and445 visible output. These are ledger measurements, not verified subscription billing or physical inference counts.

Fresh-process replay returned the identical browser receipt and export in2719ms. The probe preloaded the bridge: the production process refuses that unverified cache entry before any new native invocation, so success demonstrates the terminal-replay path did not require a new inference. The one durable native attempt record remained byte-identical. This does not claim a fresh browser rerun or new live iterative reasoning.

## Comparison limitation and next work

The plain arm has an attempt record and `plain-failure.json` with a generic `Error`, null code, no accepted revision, and no retained usage. The baseline collector records results only after proposal validation/staging, so a later error loses otherwise useful provider accounting and the specific stage. This is a confirmed observability gap in the operational experiment, not justification to infer the model's answer or loosen the Godagent boundary. The original attempt is preserved.

Before another matched comparison, retain screened, bounded per-stage baseline outcomes and usage before downstream proposal validation, test those error paths locally, and register a new symmetric sample. Do not rerun these consumed slots, erase failures, broaden model budgets, or label this one integration success as general superiority.

## Integration gate

The candidate has21 passing initial compiler/policy tests,47 passing existing transport/owner/guidance regressions, and6 passing final structured-output boundary tests. Full repository integration completed with1506 tests:1499 passed, zero failed, seven explicit browser-runtime opt-in skips,525726.885ms, exit0. The five browser cases described above are a separate real live-model application proof, not a claim that the seven opt-in tests ran in this command.

Upstream was fetched again and remained `5c698a211495d6f0d588dbb3567851fec87dab0c` before integration. Main fast-forwarded to the verified candidate with an identical tree. The merged compiler/policy/structured-output gate passed22 tests, zero failures/skips,2660.9875ms. Historical certification receipts and the unrelated untracked user `package-lock.json` were preserved. The final documentation commit records this gate without changing the tested runtime.
