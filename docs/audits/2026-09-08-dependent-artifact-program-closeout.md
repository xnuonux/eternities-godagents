# Dependent artifact program closeout

Source candidate `fc21492e85f8ad0725ddaf80f0b06315fe6c7226` passed the full
integration gate, received independent review clearance and was fast-forwarded
into main. The merged focused gate passed on the identical source tree. Source
main was pushed and its exact remote ref verified at `fc21492`; this closeout is
a documentation-only follow-up. The final documentation ref is recorded in the
external verification checkpoint after publication.

This completes the implementation and controlled proof for the
[dependent artifact program plan](../superpowers/plans/2026-09-08-dependent-artifact-program.md),
not the universal Godagents product or a live-quality certification.

## Delivered and traced

- `bdb40e9`: the existing mission coordinator exposes verified committed-step
  evidence without executing adapters or introducing a second provider journal.
- `e941271`: pure bounded recipe and resolution contracts, exact base-request
  bindings, backward-only predecessors and whole-program reservations.
- `9abdb39`: a lock-scoped, issued workflow owner shares actual identity,
  admission, policy, Realm and publication checks across missions.
- `4b20ff6`: the private artifact-operation source, exclusive bounded store and
  `program-prepare` / `program-run` CLI use the existing coordinator. Nine declared
  first-party boundary files are pinned; changed roots and sources fail closed.
- `fc21492`: real public-creator process recovery, preserved creator regression,
  a shipped example definition and operator documentation.

The [operator guide](../local-artifact-workflow.md#dependent-missions-in-one-agent)
describes the commands. One immutable workflow-3 admission supports one to eight
explicitly ordered native missions. Selected committed predecessor content and
references must fit the full 4,096-byte UTF-8 context ceiling, without truncation.
Aggregate actual publication bytes include the artifact newline. Each mission
retains distinct deterministic request/evidence identifiers, not copied identity.
Preparation is inert; program-run can execute newly absent steps and is not a
no-inference inspection command. Uncertain calls stay pending, not auto-retried.

## Verification

The implementation was test-first, including reproduced missing APIs, unsafe
mid-inference drift, source-pin omissions, directory aliasing, BOM normalization
and dense ancestor traversal. Exact RED corrections are recorded in the plan.

| Gate | Result | Elapsed |
| --- | --- | --- |
| Task-4 twelve-file focused integration | 88 passed | 48,633.5047 ms |
| Public-creator real interruption plus prior creator regression | 3 passed | 69,458.6276 ms |
| Frozen full suite, `node --test --test-concurrency=1` | 1,406 passed | 1,591,332.6419 ms |
| Six-file merged focused gate | 48 passed | 39,464.0409 ms |

All listed gates exited 0 with no failed, cancelled, skipped or todo tests.
The full suite verifies all 69 historical certification receipts and declared
links, ancestry from current HEAD, rejection of an invalid old head and immunity
to ambient Git redirection. Historical receipt/artifact files were not changed.
Full-suite and merged logs are captured below; earlier focused counts are
transcribed tool-console evidence in `verification-checkpoint.md`.

`tests/artifact-program-process-recovery.test.mjs` creates actual fresh operator
sources and kills owned children at two explicit boundaries. First completion
persisted before publication/program commit recovers that result and invokes only
the legitimate second mission. Uncertain second dispatch stays pending on fresh
CLI runs with and without a synthetic credential, with zero observed network
attempts. Both use the real 30-second dead-owner lock grace and preserve root,
genesis and first-resolution bytes. This is controlled process-death evidence,
not live networking, power-loss durability or remote exactly-once behavior.

Independent reviewer `01a07e22-93d9-7c82-981e-91ea6f493e98` cleared the source,
then closed conditional review after the actual serial full gate completed.
No critical or important finding remained in that review's scope.

## Preserved failed trial and evidence

Evidence root: `D:/00-INDEX/operations/2026-09-08-dependent-artifact-program`.

The earlier default-concurrency full-suite attempt was interrupted after Windows
`VirtualAlloc failed` and test-process failures. It has no complete aggregate
result and is not a passing gate. Its log is retained, not rewritten. The unchanged
candidate then passed the entire serial suite without omitting tests. No other
applications were closed and no system/pagefile/model settings were changed.

| File | SHA-256 |
| --- | --- |
| `full-suite.log` (interrupted default run) | `ed11ed1d2fc8c19c9ea791ee3c79788da2f45b9e0ff4fce08cfb28fcd49e11e6` |
| `full-suite-serial-fc21492.log` | `f647ba72ca12fbec27ea2203579a9019a816748a4b6e922df90d4d061b1f2239` |
| `postmerge.log` | `3968d74328155e2d60f9520e269319c8719db3df41ea9db59ac389a74703fcc3` |

## Boundaries and next useful outcome

This implementation does not enable planning, evolution, Soul, inspiration,
personal-keel mutation, new authority, global routing or Lunari integration.
The frozen Godskills effect-only dependency remains clean at
`3615b355e7dafbf2efe9e6c6bb3373458eba6119`. No skill bodies were copied, live calls
made, credentials read, spending renewed or OpenRouter fallback used. The user's
untracked `package-lock.json` and feature branch are preserved.

The dense eight-step case uses eight native calls and 108 authenticated host
constructions in fresh verification rounds. Bounded traversal is not low latency
or better model reasoning. Native-only admission has null review/revision and
Godskills bindings; actual routing refusal is tested, but an unreachable native
review-derived rejection is not claimed as qualified through fabricated receipts.
The nine-file source closure is not a transitive dependency or hostile-same-user
filesystem guarantee.

The next-study preflight is `next-comparison-preflight.md` in the evidence root.
Inspection found that the reference owner publishes JSON artifacts and the Grok
worker explicitly disables tools and permits one turn. It does not apply patches
or run app tests. A shared external patch-application/evaluation harness could
test proposals, but must not be described as autonomous app repair. Useful
governed local edit-and-test work needs a separately connected host capability.

Godskills' proposed existing incident-replay app is 11,999 bytes according to its
owner and already exposes both the defect and a working repair. It is not eligible
for a fresh held-out quality claim; excerpt fit has not been proved with an actual
compiled predecessor. Retain it only as optional compatibility material, without
rerunning or relabeling the closed study. Discover a genuinely eligible task or
implement the necessary tool/context connection before fresh efficacy testing.

Compare each model against itself, with the same substantive input, tool access,
budgets, durable progress and recovery opportunity. Separate shared-diagnostic
mechanistic continuation from endogenous diagnosis-and-repair trajectories.
Measure final behavior/regressions, actual cost and latency, not receipt validity
as quality. Keep Godskills absent in the first orchestration contrast; later skill
activation is a separate treatment. Grok 4.6's native subscription path and MiniMax
M3 remain distinct candidates. ClovAPI is not independently qualified, and the
previously exhausted MiniMax allowance is not renewed here.
