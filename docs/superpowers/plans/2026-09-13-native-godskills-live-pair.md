# Native Godskills matched live pair

> **For agentic workers:** Use `dispatching-parallel-agents` for independent acceptance work and `executing-plans` for the sequential native trials. Preserve every attempt.

**Goal:** Measure the selected Godskills consumer's contribution on one real multi-file native coding task, holding the Godagent body and host fixed.

**Architecture:** Two fresh native Pi/Grok subscription sessions start from byte-identical first-party source snapshots. The host-pinned Godskills option is the treatment; no second agent harness is introduced. Independently frozen tests score both unmodified outputs.

**Tech Stack:** Node24, existing Pi0.85.1 operator, xAI Grok4.6 OAuth subscription, existing creation/admission and certified skill pins.

**Spec:** Private task `D:/00-INDEX/operations/2026-09-13-native-godskills-live-pair/task.md`; approved comparison intent in the prior user exchange and current `go`/`resume`.

## Fixed design

- Baseline source7959e3cef2bf81fa5cebcdc76482fe2aa90d8b44. Copy tracked runtime, schemas, fixtures, selected existing tests, package metadata and native operator documentation, not raw histories or warehouse material. Verify identical manifests. Include the full runtime dependency closure to avoid the previous missing-schema intervention.
- Same creation blueprint, prompt edition, mission, tools, model response ceiling16384, tool ceiling120 and per-arm900000ms deadline. Fresh actor identifiers and filesystem roots differ only as required for isolated persistence. No automatic retry, personal Grok keel or paid API fallback.
- Control omits Godskills. Treatment uses the existing pinned release and default evidence-qualified activation, no explicit method override. Inspect routing before launch without model inference. If the route is unresolved, do not substitute another task after observing outcomes.
- Run order is randomly chosen once and recorded before either inference. Sequential runs avoid shared provider concurrency. A single pair cannot control order/cache variability or establish a general quality uplift.
- Primary endpoint: all independent frozen acceptance tests pass, existing focused regressions pass, and edits remain within the supplied scope. Secondary: acceptance fraction, elapsed time, native calls/tools, provider input/output/cache/total and incomplete usage. Do not infer dollars, hidden reasoning or guaranteed total-token caps.
- No repair of model output before scoring. No continuation prompt or replacement failed slot. Provider/timeout failures remain outcomes, with diagnostics and artifacts preserved.
- The study does not certify production Realm, autonomous review execution or Lunari. It does not compare Godagent versus plain Pi, because both arms retain the same body.

Preparation before inference:506 tracked files,3,490,002bytes per workspace,
manifest `d25ad903ff364c3213066f6b0e86f8a9a3e3e689ff077e0dc4e60cd7f6a78e24`.
Both copies pass the20 existing focused tests and actual OAuth preflight.
The separate no-inference routing probe selects Forge and Mnemosyne, both in
guardrail mode. No entrypoint body or deferred review is scheduled. Actual native
selection must still be checked against the eventual session record. Randomized
order is skills, then control. Actor IDs contain arm labels and paths differ;
this study is not blinded and those nuisance differences must be disclosed.

## Execution

- [ ] Independent acceptance author implements real filesystem/CLI checks against the task contract, validates expected baseline failures and returns concrete test limitations. Keep tests outside both agent workspaces and freeze their hashes before inference.
- [x] Prepare identical source copies, admission profiles and pinned configs using existing creation/admission APIs. Reuse the established preparation pattern rather than build a new model loop. Check existing focused regressions in each copy before launch, route treatment and record exact release/selection metadata.
- [ ] Freeze task, source, acceptance, config and execution-order digests. Verify real OAuth preflight for each arm, then use `runNativeOperator({command:'launch',config,expectedConfigDigest,prompt})` exactly once per arm. Preserve run start/result, native transcript and scope changes.
- [ ] Independently score both artifacts with unchanged acceptance and original focused tests, inspect scope violations, missing evidence and measured overhead. Publish a limited paired-result report with exact source pointers. Do not promote a candidate solely because it wins this pair.
- [ ] Reconcile upstream and commit/merge/push verified study evidence under standing authorization. Leave any product candidate outside main until its own review and integration gates pass.
