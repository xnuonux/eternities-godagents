# Godagent modular creator implementation plan

## 1. composition workflow

- add failing operator tests for exact foundation parity, canonical replacement order, one valid override, malformed categories, and blocked incompatibility;
- implement strict composition normalization over the existing catalog, preset replay, draft command, preview, and finalization functions;
- keep preset workflow behavior and projections unchanged.

## 2. browser boundary

- add failing request tests for preview, one-use acknowledgement, mismatch, replay, and finalization;
- factor shared review confirmation and atomic workspace publication instead of copying security logic;
- add the three composition routes with exact bodies and closed failures.

## 3. visual shell

- add expression and nine module selectors sourced only from the catalog;
- reset selections from a foundation preview, clear stale review on every change, and submit exact compositions;
- preserve the halo, ledger, mobile reflow, keyboard visibility, reduced motion, and self-contained asset rule.

## 4. proof and integration

- run focused tests, guarded full suite, real desktop and mobile browser acceptance, historical build reproduction, receipt hashes, and temporary certification;
- obtain independent exact-head review and fix all P0/P1/P2 findings;
- reconcile origin, fast-forward main, repeat merged gates, push, verify parity, and clean the branch/worktree.
