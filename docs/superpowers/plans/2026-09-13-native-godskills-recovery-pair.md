# Native Godskills matched pair with bounded response recovery

> **For agentic workers:** Use `executing-plans` for the sequential native trials. Preserve every attempt; no new inference harness or parallel subjects.

**Goal:** Obtain one valid, bounded comparison of the default Godskills contribution to a real native coding task after repairing native response recovery.

**Architecture:** Two fresh admissions use the current Godagent/Pi host but receive identical exports of the untouched pre-implementation source. The only treatment is the existing optional, automatically selected Godskills package. Frozen independent tests score both outputs before owner repair.

**Tech Stack:** Node24, qualified Pi0.85.1, xAI Grok4.6 OAuth subscription, existing creation/admission and certified Godskills pins.

**Spec:** Same task contract as `docs/superpowers/plans/2026-09-13-native-godskills-live-pair.md`, interpreted with the later native recovery direction in `AGENTS.md`. New private task and study files: `D:/00-INDEX/operations/2026-09-13-native-godskills-recovery-pair`.

## Fixed conditions and limits

- Workspace source: `7959e3cef2bf81fa5cebcdc76482fe2aa90d8b44`, exported from Git, not copied from current solved source or an old trial workspace. Full original dependency closure is retained: 506 selected tracked files. No current solution, owner review, extra acceptance checks or personal histories enter the subjects' source copies.
- Host runtime: `3ec7b3d8d9ec2f628eb3cce7871eeec47d505b95`. Verify no drift in runtime/source dependencies; freeze six key host-module hashes and five installed SDK/adapter hashes before use. Source snapshots intentionally predate host recovery; the host executing the subjects does not.
- Same blueprint, prompt edition, mission, first task text, native model and effects in both arms. Fresh actor/session identifiers and workspace paths differ. No personal Grok keel or global provider change.
- Per arm: 900,000ms deadline, 120 native tools, model response setting16,384 and explicit `limits.maxProviderRetries:1`. This is one native retry per failed response within the original deadline/tool ceiling, not a total-token or billing guarantee. Quota/auth failure has no fallback.
- One owner launch per arm, no continuation prompt or replacement failed slot. Sequential, randomly ordered once before dispatch. Record retry events, failures, usage unknowns and actual skill delivery. Preserve outcomes even if the question remains inconclusive.
- Control omits Godskills. Treatment uses existing pinned roots and automatic evidence-qualified activation with no explicit method request and no review executor. Do not force a method or rewrite a skill after seeing a route or result. This measures the default selected package, not every possible Godskill.
- Primary endpoint: normally settled native turn,23/23 frozen acceptance,20/20 unchanged focused regressions, no source scope violation. Acceptance retains the original20 independently authored tests and adds3 explicit contract checks for primitive status and impossible UTC dates. Report original20 and additional3 outcomes separately.
- Secondary observations: acceptance fraction, actual native calls/tools, elapsed time, provider-reported input/output/cache/total, retry count and incomplete usage. Do not infer dollars or hidden reasoning, reconstruct missing totals, or compare failed subtotals as complete consumption.
- Native tools execute as the OS user. Task instructions and private acceptance placement are experimental separation, not an OS sandbox. No network, dependency install, sibling/canonical repository access, credentials, unrelated files or personal memories are authorized to the subjects.
- No model output is repaired before scoring, no candidate is automatically adopted, and no trust root or skill body changes during the pair. The canonical history feature is already integrated. This is evidence collection, not redundant product implementation.
- One pair is unblinded and unreplicated; model nondeterminism, order, server/cache conditions and arm identifiers remain confounds. Do not declare a general quality uplift or failure from this result. Production Realm, autonomous review execution, Soul/Inspiration and Lunari remain outside scope.

## Execute inline

- [x] Prepare the two new source exports and admissions with adapted existing `prepare.mjs`. Check identical manifests,20 original baseline regressions, real OAuth preflight and no-inference treatment route. Do not rerun any old study script.
- [x] Validate the23 frozen checks against the current independently reviewed reference and both untouched baselines. Reference must pass23; baseline failures must correspond to the absent query behavior. Review test cases and script differences, then run the new `freeze.mjs` once and record hashes/order before inference.
- [ ] Invoke the existing `runNativeOperator` via the new `run.mjs` once per arm in frozen order. Allow only the registered native retry behavior. Wait for complete terminal evidence; never duplicate in-flight inference.
- [ ] Run `score.mjs` once on each untouched output. Inspect actual selected binding, native transcript event summaries and scope. Publish exact paired outcomes, remaining defects, limits and the smallest evidence-backed next action.
- [ ] Reconcile upstream; independently review the evidence interpretation, merge and push study documentation under standing approval. No full product suite is needed unless runtime/source changes. Any later repair is a separately verified milestone, not a rewrite of these trials.

## Frozen preparation, before inference

Both source copies contain506 files,3,490,002bytes. Source-manifest digest:
`d25ad903ff364c3213066f6b0e86f8a9a3e3e689ff077e0dc4e60cd7f6a78e24`.
Both pass20/20 original regressions; both score9/23 acceptance before work with
the same14 missing-feature failures. The independent reviewed reference passes
23/23. The original task/helper/20-test bytes retain their historical hashes;
the three additional edge checks are versioned separately, hash
`5cf69d150ad3c74a28d1dddc0e03424c6f698fecb6e453f0cae03f90ebcf19f7`.

Freeze timestamp: `2026-09-14T02:54:13.896Z`. Randomized order: **control, skills**.
Both actual preflights report Pi0.85.1, xAI/grok-4.6, OAuth subscription and
the same native grant. Config digests:

- Control: `0001da4256c178f3877819b0912e76d731341dc91c78bdd17c5f847dc31f1b87`.
- Skills: `e6b69ec6bf4055bd5d455f1d2e07d416465bbac478391d640fa74e19390ccfec`.

The separate no-inference treatment probe selects Forge/Mnemosyne guardrails,
no method body and no scheduled review. Release digest:
`c72a0ce54f6c42f1542068e8fe61e046500587973b716f4c15effaac8c862f5f`.
Actual delivered selection must still be verified from the native session.
Godskills main/origin remains `d0781a91a82898cc808e7fc681f145fdd972dade`.
