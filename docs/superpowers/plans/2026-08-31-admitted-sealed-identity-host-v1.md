# admitted sealed identity host v1 implementation plan

1. add closed identity-host policy schema, descriptor validation, semantic
   ceilings, canonical loading, and policy digest tests.
2. extract the existing admission-tree and binding checks into one shared
   boundary without changing legacy launcher behavior.
3. add failing tests for policy pinning, admission identity, request ceilings,
   dependency descriptors, review pairing, and legacy non-regression.
4. implement the separate programmatic admitted sealed identity launcher and
   bind the certified routing-evidence classifier before vessel construction.
5. prove full-loop execution, interruption recovery, exact terminal replay, and
   zero authority expansion with real pinned Godskills processes.
6. add deterministic fixture, append-only receipt, certification document,
   package command, ledger row, and lineage gate.
7. run focused tests, all tests, release gates, two receipt reproductions, and an
   inline adversarial review.
8. reconcile origin, fast-forward main, push, verify equality, clean the exact
   worktree and branch, then select the concrete model-transport phase.
